{
  "name": "@mbarde/volto-image-crop-widget",
  "version": "0.3.3",
  "description": "Volto add-on: Extends Voltos FileWidget to be able to crop images in-place.",
  "main": "src/index.js",
  "license": "MIT",
  "keywords": [
    "volto-addon",
    "volto",
    "plone",
    "react",
    "react-image-crop"
  ],
  "repository": {
    "type": "git",
    "url": "git@github.com:mbarde/volto-image-crop-widget.git"
  },
  "scripts": {
    "i18n": "rm -rf build/messages && NODE_ENV=production i18n --addon"
  },
  "dependencies": {
    "@plone/scripts": "*",
    "react-image-crop": "10.0.0"
  }
}
